steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run',
        'deploy',
        'server',
        '--source', '.',
        '--region', 'us-central1',
        '--platform', 'managed',
        '--allow-unauthenticated',
        '--set-env-vars', 'NODE_ENV=production',
        '--set-secrets', '.env=server-env:latest'
      ]

options:
  logging: CLOUD_LOGGING_ONLY
